# UX Baseline Notes

## 2026-06-08 Pre-Expansion Baseline

Kinetriq `3.4.2` build `26` was working as expected before beginning the next set of broader user-experience changes.

Use `3.4.2 (26)` as the known-good fallback when reviewing regressions from the `3.4.3` work around exercise selection, customizable overlays, analyzed-video playback, and Home screen FAQ layout.
