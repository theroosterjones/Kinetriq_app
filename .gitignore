# Xcode
build/
DerivedData/
*.xcuserstate
*.xcuserdatad/
*.xccheckout
*.xcscmblueprint
*.moved-aside
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3

# Swift Package Manager
.build/
Packages/
Package.resolved
.swiftpm/

# CocoaPods
Pods/

# MediaPipe model files (large binaries)
*.task

# macOS
.DS_Store
*.swp
*~.nib

# IDE
.idea/
.vscode/
*.code-workspace

# Kinetriq local
.derivedData/
.sourcepackages/
Config/KinetriqSecrets.xcconfig

# Coach dashboard runtime config. The anon key is public by design, but the repo
# convention is that project identifiers are not committed — copy
# web/coach/config.example.js to web/coach/config.js when deploying.
web/coach/config.js

# Supabase CLI local scratch / linked-project state
supabase/.temp/
supabase/.branches/

# Secrets / signing keys — NEVER commit these
# App Store Connect / Apple auth keys (.p8), provisioning profiles, certs
*.p8
AuthKey_*.p8
*.p12
*.mobileprovision
*.cer
*.certSigningRequest
